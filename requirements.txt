tqdm
xopen
pydantic
pytest
regex

# For running hosted API models (OpenAI / Anthropic / Google / OpenAI-compatible).
# These are lightweight and have no GPU/CUDA dependencies, so a plain
# `pip install -e .` is enough for the API-based experiments. Each SDK is only
# imported when its provider is actually used.
openai>=1.40.0
anthropic>=0.40.0
google-genai>=0.3.0
python-dotenv>=1.0.0

# Checks style, syntax, and other useful errors.
flake8==5.0.4

# Automatic code formatting
black==22.10.0

# For creating git precommit hooks
pre-commit

# ----------------------------------------------------------------------------
# NOTE: The original open-weight local-GPU models (MPT, LongChat, LLaMA-2) require
# heavy, CUDA-specific dependencies (torch, vllm, flash-attn, ...). Those are NOT
# listed here so that `pip install -e .` stays installable without a GPU. To
# reproduce the local-GPU runs, install the optional extra instead:
#     pip install -e .[local-gpu]
# which pulls in requirements-local-gpu.txt.
# ----------------------------------------------------------------------------
